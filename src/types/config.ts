export interface Config {
    width:number | string,
    height:number | string,
    titleSize:number,
    ratingSize:number,
    runtimeSize:number,
    fontStyle: string,
    border:string,
    hover:boolean
}